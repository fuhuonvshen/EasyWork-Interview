# EasyWork → 面试助手 改造方案

> 状态：**已实施 Phase 1 + Phase 2 主体**（2026-07 代码改造完成，待 CI 编译验证）
> 目标：把 EasyWork（会议纪要 + 办公助手）重塑为**专门服务求职者面试备考**的桌面应用
> 决策已确认：① 用户定位 = **求职者备考**；② Agent 形态 = **混合**（主控台入口卡片 + 业务页面内嵌 AI）；③ 先出方案文档再实施

## ✅ 实施进度

- [x] **Phase 1 产品重塑**
  - [x] DB 迁移：meetings 加 kind/company/position/stage/score；scheduled_meetings 加 stage；agent_conversations 加 type/ref_id；新增 interview_questions / interview_assessments 表（`models.rs` / `repo.rs` / Python `database.py` 幂等迁移）
  - [x] Rust 命令：`generate_minutes` 面试模式、`update_meeting_interview_info`、`save/get_interview_assessment`、`interview_question_create/list/delete`、`agent_create_conversation(type, ref_id)`、`agent_update_conversation_meta`、日程 stage
  - [x] 面试纪要模板（`summary/gen.rs` `generate_interview_minutes` + `template.rs` 求职者视角五维复盘）
  - [x] 工作台六宫格（面试记录/日程/复盘报告/面试助手/简历顾问/题库与模拟）+ 高级化设计语言（`Workbench.tsx`）
  - [x] 面试语义：录制面板（公司/岗位/阶段+合规提示）、历史列表徽标、详情页 AI 深度复盘按钮、日程阶段、复盘周/月报
  - [x] 说话人标签兼容"候选人/面试官"（`HistoryDetail.tsx` / `TodayView.tsx`）
- [x] **Phase 2 Agent 大改**
  - [x] 面试助手人设 + 分角色提示词（`prompt.py`：mock/review/resume）
  - [x] 角色分支（`chat.py` `_conversation_setup` + `_extract_assessment`；review 注入转写上下文，mock 结束自动建面试记录）
  - [x] 记忆重构（`memory.py` 求职档案语境）
  - [x] 新工具：`interview_summary`、`question_bank`；registry 按角色过滤（屏蔽 email）
  - [x] 前端：角色选择弹窗、侧边栏角色分组、mock 模式横幅/计时/结束按钮、内嵌唤起（复盘/简历/模拟入口自动发首条消息）
- [ ] **待办**：Phase 3 增强（简历 PDF/DOCX 解析、复盘报告图表、题库管理页、全局悬浮助手）；Rust 侧待 GitHub Actions CI 编译验证
- [ ] UI 预览：`docs/agent-ui-preview.html`（静态效果稿，与实施后的真实界面设计语言一致）

---

## 一、改造总原则

1. **不重写，做语义迁移**：录制 → 转写 → AI 纪要 → 导出的 pipeline 是现成资产，整体平移给"面试记录"，改动集中在领域文案、数据字段、Agent 人设与流程。
2. **目录名尽量不动，动对外语义**：`minutes/`、`schedule/` 等目录重命名会引发 import 路径、Tauri command、样式引用的大面积改动，收益低。**第一阶段只改 UI 文案、类型定义、数据库字段**；目录重命名列为可选清理项（阶段末）。
3. **数据兼容优先**：`meetings` 表加 `kind` 列（`meeting` / `interview`），存量会议数据保留可查，不做破坏性迁移。
4. **Agent 是能力不是目的地**：模拟面试、简历分析这类对话场景走"面试助手"入口；复盘、点评这类操作以内嵌按钮 + 带上下文唤起。

---

## 二、信息架构（改造后）

```
App 视图（src/App.tsx）
├── workbench  →  面试工作台（首页）
│     ├── 🎙 面试记录   （原 minutes，含 今日/历史/日程/复盘报告 四 tab）
│     ├── 📅 面试日程   （原 schedule，含阶段状态机）
│     ├── 📊 复盘报告   （原 reports，投递统计/通过率/公司对比）
│     ├── 🤖 面试助手   （原 agent 入口，保留为卡片）
│     ├── 📝 简历顾问   （新增卡片 → 直接创建 resume 类型对话）
│     └── 📚 题库与模拟 （新增卡片 → 题库管理 + 创建 mock 类型对话）
├── interviews →  面试记录应用（原 minutes 语义替换）
├── agent      →  面试助手（原 AgentApp，支持角色化对话）
└── feedback   →  意见反馈（降级为底部小入口，保留原页）
```

- **AI 内嵌点**（混合形态的关键）：
  - 面试记录详情页（HistoryDetail）：加"让 AI 深度复盘"按钮 → 创建/跳转 `review` 对话并注入该面试上下文
  - 录制结束弹窗（TodayView 生成纪要后）：加"生成面试评估"按钮
  - 面试日程：面试前提醒文案可带"AI 押题"入口
- 托盘菜单、提醒弹窗（ReminderModal）：文案同步改面试语义。

---

## 三、数据模型变更（Rust `models.rs` + `repo.rs` 迁移）

```sql
-- ① meetings 表（面试语义字段，全部可选，兼容存量）
ALTER TABLE meetings ADD COLUMN kind TEXT NOT NULL DEFAULT 'meeting';   -- 'meeting' | 'interview'
ALTER TABLE meetings ADD COLUMN company TEXT;                            -- 公司
ALTER TABLE meetings ADD COLUMN position TEXT;                           -- 岗位
ALTER TABLE meetings ADD COLUMN stage TEXT;                              -- 'phone'|'online'|'onsite'|'mock'|'offer'
ALTER TABLE meetings ADD COLUMN score INTEGER;                           -- AI 评估总分（0-100）

-- ② scheduled_meetings（日程也分阶段）
ALTER TABLE scheduled_meetings ADD COLUMN stage TEXT NOT NULL DEFAULT 'phone';

-- ③ agent_conversations（角色化）
ALTER TABLE agent_conversations ADD COLUMN type TEXT NOT NULL DEFAULT 'general'; -- 'general'|'mock'|'review'|'resume'
ALTER TABLE agent_conversations ADD COLUMN ref_id TEXT;                          -- 关联 interviews/meetings.id（上下文注入用）

-- ④ 新增题库表
CREATE TABLE IF NOT EXISTS interview_questions (
  id TEXT PRIMARY KEY,
  category TEXT NOT NULL,          -- 'java'|'算法'|'数据库'|'HR'|'项目'...
  difficulty TEXT NOT NULL,        -- 'easy'|'medium'|'hard'
  question TEXT NOT NULL,
  expected_answer TEXT,
  created_at TEXT NOT NULL
);

-- ⑤ 新增面试评估表（AI 输出结构化落库）
CREATE TABLE IF NOT EXISTS interview_assessments (
  id TEXT PRIMARY KEY,
  interview_id TEXT NOT NULL,      -- FK → meetings.id
  dimensions TEXT NOT NULL,        -- JSON：{专业技能,沟通表达,逻辑思维,岗位匹配,潜力}
  score INTEGER,
  summary TEXT,
  created_at TEXT NOT NULL
);
```

迁移方式沿用 `repo.rs` 现有的 `ALTER TABLE ... ADD COLUMN` 模式（参考 `schedule_id`、`pinned` 的写法），幂等可重跑。

---

## 四、Phase 1 — 产品重塑（语义迁移）

### 4.1 前端

| 文件 | 改动 |
|---|---|
| `src/App.tsx` | 视图名 `minutes` → `interviews`（内部）；`startup_page` 设置项同步；新增"简历顾问/题库"两个入口路由 |
| `src/workbench/Workbench.tsx` | 卡片重构为六宫格（见信息架构）；删除"共享屏幕"占位；反馈降级底部 |
| `src/types.ts` | `MinutesTab` → `InterviewTab`；`MeetingRow` 增加 `kind/company/position/stage/score` 可选字段；`ScheduledMeeting` 加 `stage`；`AgentConversationSummary` 加 `type` |
| `src/minutes/index.tsx` | 对外文案"会议"→"面试"；四 tab 文案：今日→面试记录、历史→历史、日程→面试日程、报告→复盘报告 |
| `src/minutes/components/MinutesSidebar.tsx` | 文案与图标语义 |
| `src/minutes/recording/TodayView.tsx` | 录制表单增加"公司/岗位/面试阶段"字段；标题自动模板 `{公司}-{岗位}-{阶段}`；**新增录音合规提示**（录制他人需同意） |
| `src/minutes/history/MeetingListView.tsx` | 列表卡片显示公司/岗位/阶段/评分徽标；按 kind 筛选 |
| `src/minutes/history/HistoryDetail.tsx` | 显示面试元信息 + 评分；加"让 AI 深度复盘"按钮 |
| `src/schedule/*` | 日程表单加阶段下拉（投递/一面/二面/终面/Offer）；文案改面试 |
| `src/minutes/reports/*` | 周报/月报 → 面试复盘：投递统计、各公司表现、通过率 |
| `src/agent/AgentSidebar.tsx` | 对话列表按 type 分组/筛选 |
| `src/agent/AgentApp.tsx` | 新建对话时选择角色（通用/模拟面试/复盘/简历顾问） |

### 4.2 Rust

| 文件 | 改动 |
|---|---|
| `src-tauri/src/database/models.rs` | 按第三节加字段与两张新表结构体 |
| `src-tauri/src/database/repo.rs` | 幂等迁移 SQL；`create_meeting` 增加面试字段参数；`create_conversation` 增加 `type`/`ref_id` |
| `src-tauri/src/minutes/meeting.rs` | `create_meeting` / `update_meeting_title` 扩展面试字段；新增 `update_meeting_interview_info`、`get_meeting_assessment` |
| `src-tauri/src/minutes/schedule.rs` | 日程加 stage |
| `src-tauri/src/minutes/report.rs` | `generate_report` 的 prompt 换面试复盘模板（见 4.4） |
| `src-tauri/src/summary/gen.rs` | 纪要生成模板改为**面试纪要**：结构化输出（面试概况 / 问答要点 / 候选表现 / 评分维度） |
| `src-tauri/src/agent/commands.rs` | `agent_create_conversation` 带 `type`、`refId`；新增题库 CRUD 命令 |
| `src-tauri/src/lib.rs` / `state.rs` | 模块声明与状态基本不变（无重命名），仅新增命令注册 |

### 4.3 说话人区分标签

`diarization/mod.rs` 目前标注"我 / 发言人 / 参会者_N"。面试场景建议：
- 默认映射："我"（麦克风侧，即本地用户=求职者）→ **候选人**；其余 → **面试官_N**
- 在设置项加"录音人身份"开关（候选人 / 面试官），切换标签映射，不改引擎。

### 4.4 面试纪要模板（`summary/gen.rs`）

```
## 面试概况（公司/岗位/阶段/时长）
## 问答要点（逐题：问题 → 回答摘要 → 表现评价）
## 表现亮点
## 待改进项
## 评分（专业技能/沟通表达/逻辑思维/岗位匹配/潜力，各 0-10）
## 下一步建议
```

---

## 五、Phase 2 — Agent 大改（核心）

### 5.1 人设与提示词（`py_backend/llm/prompt.py`）

- `system_prompt()` 删除"啤酒包装生产助手"，改为**面试助手**人设：
  - 职责：① 模拟面试官出题与点评；② 基于转写生成复盘评估；③ 简历分析与岗位匹配；④ 面试问题生成；⑤ 跟进待办管理。
  - 保留现有硬性要求（绝对日期、表格输出、`[待确认]` 标注、不编造）。
- 新增**分角色提示词**：`mock_prompt()` / `review_prompt()` / `resume_prompt()`，由 `chat.py` 按 `conversation.type` 选用。

### 5.2 对话角色系统（`chat.py` + `context.py` + `memory.py`）

| type | 行为 |
|---|---|
| `general` | 现有 ReAct 流程不变（人设换成面试助手） |
| `review` | 读 `ref_id` 对应的 meetings/transcripts/minutes → 摘要注入 system → 输出 ```` ```assessment ```` JSON → 落库 `interview_assessments` |
| `resume` | 上传简历文件（新增 .pdf/.docx 支持）→ 注入简历内容 → 输出改进建议 |
| `mock` | **新状态机流程**（见 5.3），不走标准 ReAct |

- `context.py`：`build_context` 增加 `conversation_type` / `ref_id` 参数，按类型注入领域上下文。
- `memory.py`：抽取 prompt 从"生产偏好"改为**求职档案**（目标岗位/意向城市薪资/面试进度/公司反馈/常犯错误）。

### 5.3 模拟面试流程（mock，重点新增）

**前端**（`AgentChat.tsx` 增加 mock 模式）：
```
[新建 mock 对话] → 填目标岗位 + 粘贴/输入 JD
→ Agent 流式出第 1 题（题目卡片，附考察点标签）
→ 用户文字作答（可计时）
→ Agent 点评 + 追问 / 下一题
→ 循环 N 轮（可配置，默认 5 题）
→ 用户点"结束面试" → Agent 生成总结评估（各维度打分 + 建议）
```

**后端**（`chat.py` 增加 mock 分支）：
- 会话状态存 `agent_messages`（role 标记 `mock_question` / `mock_answer`），不走多轮 ReAct；
- 出题阶段工具面收窄（仅 `question_bank` 可选，或纯 LLM 生成）；
- 结束时输出 ```` ```assessment ```` → 解析落库（`meetings.kind='interview' AND stage='mock'`）。

### 5.4 技能/工具系统（`py_backend/tools/handlers/`）

| 动作 | 工具 | 说明 |
|---|---|---|
| 保留 | `todo` | 跟进事项（投简历/刷题/感谢信） |
| 保留 | `xlsx` | 投递记录表 / 评分表 |
| 保留 | `execute_python` | 数据分析（各公司对比、通过率） |
| 降级 | `email` | 相关性低，从默认工具列表移除（保留实现） |
| **新增** | `interview_summary.py` | 读指定面试的转写/纪要（直连共享 SQLite） |
| **新增** | `question_bank.py` | 题库 CRUD（`interview_questions` 表） |
| **新增** | `resume_parse.py` | 简历解析（PDF/DOCX → 文本，新依赖 `pdfplumber` + `python-docx`，需评估 PyInstaller 打包体积） |
| 机制 | `registry.py` | `get_tool_definitions()` 增加按 `conversation.type` 过滤（如 mock 对话不暴露 `email`/`xlsx`） |

### 5.5 内嵌唤起（混合形态落地）

新增 Rust 命令 `agent_open_contextual(type, refId)`（或前端直接 `agent_create_conversation` + 首条消息预填），实现：
- 面试详情页"让 AI 深度复盘" → 创建 `review` 对话 + `ref_id` → 跳转 Agent 视图并自动发一条"请复盘这场面试"。
- 复盘报告页"生成面试问题" → 创建 `general` 对话 + 预填岗位上下文。

---

## 六、Phase 3 — 增强（后续迭代）

1. **简历解析渲染**：上传 PDF/DOCX 简历后可视化预览 + 简历顾问逐节建议。
2. **复盘报告升级**：投递漏斗（投递→一面→二面→Offer）、公司维度对比图（matplotlib 已有）、通过率趋势。
3. **题库管理页**：`src/settings/` 或独立页管理 `interview_questions`（导入/导出）。
4. **全局悬浮助手**（可选）：Ctrl+Space 唤起浮窗，复用 Agent 侧边服务。
5. **TTS 模拟面试**（可选）：本地 TTS 语音出题，更接近真实面试。

---

## 七、实施顺序与验收

| 步骤 | 内容 | 验收标准 |
|---|---|---|
| 1 | DB 迁移（models.rs / repo.rs） | 老库升级不报错，`kind/type` 默认值正确 |
| 2 | Workbench 六宫格 + 视图路由 | 首页卡片可进入各场景 |
| 3 | 面试记录语义（TodayView/列表/详情/纪要模板） | 录一段音频 → 生成面试纪要含评分维度 |
| 4 | 日程/复盘报告语义 | 日程带阶段；周报为面试复盘 |
| 5 | Agent 人设 + conversation type | 通用对话以面试助手身份回答 |
| 6 | mock 模拟面试流程 | 5 轮问答 → 结束 → 评估落库 |
| 7 | review 内嵌复盘 | 详情页一键复盘，上下文正确注入 |
| 8 | 题库 + 简历（可选） | 题库 CRUD；简历分析可用 |

---

## 八、风险与注意事项

1. **录音合规**：录制真实面试（尤其线上面试的系统音频环回）涉及对方同意，录制面板必须加提示文案。
2. **旧数据兼容**：存量 meetings 按 `kind='meeting'` 保留，列表默认筛选 `kind='interview'`，设置项可"显示旧会议数据"。
3. **PyInstaller 打包体积**：新增 PDF/DOCX 解析依赖会增大 sidecar 体积，需在阶段 3 评估。
4. **mock 会话的 LLM 成本/延迟**：本地小模型（llama.cpp）出题质量有限，可优先走在线 API；状态机需处理用户中途退出。
5. **目录不重命名**：`minutes/`、`schedule/` 内部命名保留，靠文案和类型别名呈现面试语义；若坚持重命名，作为独立清理任务单独排期（影响所有 import 与 Tauri 注册）。

---

## 附：涉及文件总清单（按阶段）

**Phase 1（约 18 个文件）**：`src/App.tsx`、`src/workbench/Workbench.tsx`、`src/types.ts`、`src/minutes/*`（index/TodayView/MeetingListView/HistoryDetail/MinutesSidebar/reports）、`src/schedule/*`、`src/ReminderModal.tsx`、`src-tauri/src/database/{models,repo}.rs`、`src-tauri/src/minutes/{meeting,schedule,report}.rs`、`src-tauri/src/summary/gen.rs`、`src-tauri/src/agent/commands.rs`、`src-tauri/src/lib.rs`

**Phase 2（约 14 个文件）**：`py_backend/llm/{prompt,chat,context,memory}.py`、`py_backend/data/{database,models}.py`、`py_backend/tools/registry.py`、`py_backend/tools/handlers/{interview_summary,question_bank,resume_parse}.py`、`src/agent/{AgentApp,AgentSidebar,AgentChat}.tsx`、`src-tauri/src/agent/commands.rs`、`src-tauri/src/database/repo.rs`

**Phase 3（约 8 个文件）**：简历解析渲染、复盘报告升级、题库管理页、悬浮助手
